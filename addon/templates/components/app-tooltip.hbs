{{#if enabled}}
	{{#-in-element (get-element-by-tag "app-render")}}
		<app-tooltip id={{this.uniq}} side={{this.side}} visible={{this.visible}} class={{this.itemClass}}>
			<arrow></arrow>
			{{~yield~}}
		</app-tooltip>
	{{/-in-element}}
{{/if}}